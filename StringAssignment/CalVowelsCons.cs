using System;
using System.Text;
//2.Calculate no of vowels, consonants  , digits and special symbol from given String


namespace StringAssignment
{
    public class CalVowelsCons
    {
        /*static void Main(string[] args)
        {
            String str = "kshitija@123#%";
            Console.WriteLine(str);
            char ch;
            int c1 = 0, c2 = 0,c3=0,c4=0;
            for (int i = 0; i < str.Length; i++)
            {
                ch = str[i];
                if (ch >= 'a' && ch <= 'z' || ch >= 'A' && ch <= 'Z')
                {
                    if (ch == 'a' || ch == 'A' || ch == 'e' || ch == 'E' || ch == 'i' || ch == 'I' || ch == 'o' || ch == 'O' || ch == 'u' || ch == 'U')
                    {
                        c1++;
                    }
                    else
                    {
                        c2++;
                    }
                }

                else if (ch >= '0' || ch <= '9')
                {
                    c3++;
                }

                else
                {
                    c4++;
                }

            }
            Console.WriteLine("Count Of Vowels in String :"+c1);
            Console.WriteLine("Count Of Cosonants in String :" + c2);
            Console.WriteLine("Count Of Digits in String :" + c3);
            Console.WriteLine("Count Of Special Symbols in String :" + c4);
        }*/
    }
}
