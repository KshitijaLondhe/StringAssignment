using System;
using System.Text;
//3.Check occurrences of particular  characters,
//how many times it’s there and which diff positions.


namespace StringAssignment
{
    public class CharOccurance
    {
       /* static void Main(string[] args)
        {
            Console.WriteLine("Enter the string :");
            String str1 = Console.ReadLine();
            Console.WriteLine("Enter character to search :");
            String ch1 = Console.ReadLine();
            char ch = ch1[0];
            int count = 0;
            int index = 0;
            for (int i = 0; i < str1.Length; i++)
            {
                if (ch == str1[i])
                {
                    count++;
                    index = str1.IndexOf(ch);
                }
            }
            Console.WriteLine(ch + " occurs in string " + count + " times");
            Console.WriteLine("Index of "+ch+" is "+index);

        }  */
    }
}
